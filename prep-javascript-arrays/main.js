const languages = ['html', 'css', 'Javascript', 'react', 'node', 'postgres'];

console.log(languages);

var firstElement = languages[0];

console.log('the first element of the arry is', firstElement);

var thridElement = languages[2];

console.log('the 3rd element of the array is', thridElement);

var length = languages.length;

console.log('the length of the arry is', length);

var lastElement = languages[length - 1];

console.log('the last element in the arry is', lastElement);
