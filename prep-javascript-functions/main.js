function addTwoNumbers(Num1, num2) {
  return (Num1) + (num2);
}

var addTwoNumbersResult = addTwoNumbers(2, 2);

console.log('addTwoNumbers Excercise:', addTwoNumbersResult);

function convertHoursToMinutes(hours) {
  return (hours) * (60);
}

var convertHoursToMinutesResult = convertHoursToMinutes(2);

console.log('converHoursToMinutes Excercise:', convertHoursToMinutesResult);

function getGreeting(names) {
  return 'Hello' + ' ' + (names) + '!';
}

var getGreetingResult = getGreeting('World');

console.log('getGreeting Excercise:', getGreetingResult);

function addAndMultiplyBy5(num1, num2) {
  return (num1 + num2) * (5);
}

var addAndMultiplyBy5Result = addAndMultiplyBy5(10, 5);

console.log('addAndMultiplyBy5 Excercise:', addAndMultiplyBy5Result);

function subtractTwoNumbers(num1, num2) {
  return (num1) - (num2);
}

var subtractTwoNumbersResults = subtractTwoNumbers(22, 7);

console.log('subtracTwoNumber Excercise:', subtractTwoNumbersResults);

function getCircumference(number) {
  return (2) * (Math.PI) * (number);
}

var getCircumferenceResults = getCircumference(5);

console.log('getCircumference Excercisel', getCircumferenceResults);

function getFullName(firstName, lastName) {
  return (firstName) + ' ' + (lastName);
}

var getFullNameResults = getFullName('Juan', 'Ramirez');

console.log('getFullName Excercise:', getFullNameResults);

function cube(number) {
  return (number) * (number) * (number);
}

var cubeResults = cube(5);

console.log('cube Excercise:', cubeResults);
